;-------------------;
;;; R Programming ;;;
;-------------------;

;; ; R emacs mode
;; (require 'ess-site)
;; ; automatically get the correct mode 
;; auto-mode-alist (append 
;;                  (list '("\\.R$" . R-mode)
;;                        '("\\.r$" . R-mode)
;;                        )
;;                  auto-mode-alist)
;; (setq-default inferior-R-program-name "R") 

(provide 'r-settings)
