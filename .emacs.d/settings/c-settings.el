;-------------------;
;;; C Programming ;;;
;-------------------;

(setq c-basic-offset 8)
(setq c-default-style (quote (
    (c-mode . "bsd") 
    (java-mode . "java") 
    (awk-mode . "awk") 
    (other . "gnu"))))

(provide 'c-settings)
